# Normalize every text file to LF in the repository (git blobs) and on
# checkout, regardless of a contributor's local core.autocrlf/core.eol. Git
# auto-detects binary files, so this is safe even though the pattern is
# global — see docs/refactor-progress-ia/T11.1-line-endings.md.
* text=auto eol=lf
