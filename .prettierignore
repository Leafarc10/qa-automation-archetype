# Dependencies
node_modules/

# Generated / artifacts
reports/
test-results/
playwright-report/
blob-report/
coverage/
*.tsbuildinfo

# Historical refactor reports: not user-facing documentation, and reformatting
# hundreds of already-approved lines would add noise with no value.
docs/refactor-progress/

# Markdown documentation (README, AGENTS*.md): prose docs are reviewed and
# reformatted deliberately, not automatically — an unrequested Prettier diff
# to prose is more noise than signal.
*.md

# Editor-specific, not a build/tooling config file.
.vscode/

# Lockfile: managed by npm, not meant to be hand-formatted.
package-lock.json
