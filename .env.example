# Application
# Public, free, no-login demo site used by the T13 UI example (features/example/example.feature).
BASE_URL=https://playwright.dev

# Browser
HEADLESS=true
BROWSER=chromium
DEFAULT_TIMEOUT_MS=120000

# Database (optional)
DB_ENABLED=false
DB_USER=
DB_PASSWORD=
DB_CONNECT_STRING=
ORACLE_CLIENT_LIB_DIR=