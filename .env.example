# Application
# Public demo site used by the canonical UI example, the SauceDemo checkout scenarios
# (features/sauceDemo/checkout.feature).
SAUCEDEMO_BASE_URL=https://www.saucedemo.com

# Browser
HEADLESS=true
BROWSER=chromium
DEFAULT_TIMEOUT_MS=120000

# Database (optional)
DB_ENABLED=false
DB_USER=
DB_PASSWORD=
DB_CONNECT_STRING=
ORACLE_CLIENT_LIB_DIR=